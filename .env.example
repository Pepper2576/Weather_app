# HTTP Server Configuration
HOST=0.0.0.0
PORT=8080

# Database Configuration (Docker)
# Note: When running with docker-compose, DB_HOST should be 'postgres' (the service name)
# When running locally without Docker, use 'localhost'
DB_HOST=postgres
DATABASE=weather_db
USER=weather_user
PASSWORD=change_this_secure_password